{
  "name": "server",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
	"db:up": "docker compose up -d"
  },
  "dependencies": {
    "hono": "^4.11.7"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}